# Environment Variables Example
# Copy this file to .env when local environment variables are needed.
# Do not put real API keys or secrets in this file.

# Resend Email API Key
# Used by api/contact.js to send contact form emails through Resend.
# The real value should be configured in Vercel Project Settings → Environment Variables.

RESEND_API_KEY=your_resend_api_key_here